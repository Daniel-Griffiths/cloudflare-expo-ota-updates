{
  "name": "@cloudflare-expo-ota/worker",
  "version": "1.0.0",
  "private": true,
  "description": "OTA update server on Cloudflare Workers",
  "license": "MIT",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "cli": "tsx cli/index.ts",
    "db:generate": "drizzle-kit generate",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "@inquirer/prompts": "^8.3.2",
    "drizzle-orm": "^0.45.1",
    "hono": "^4.12.8",
    "mrmime": "^2.0.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.13.2",
    "@cloudflare/workers-types": "^4.20260317.1",
    "@types/node": "^25.5.0",
    "@types/yargs": "^17.0.35",
    "@vitest/ui": "^4.1.0",
    "chalk": "5.6.2",
    "cli-table3": "^0.6.5",
    "drizzle-kit": "^0.31.10",
    "ora": "^9.3.0",
    "tsx": "^4.21.0",
    "vitest": "^4.1.0",
    "yargs": "^18.0.0"
  },
  "cloudflare": {
    "bindings": {
      "BUCKET_URL": {
        "description": "**(Required)** The public URL for your R2 bucket (custom domain or R2.dev subdomain). [Setup guide](https://github.com/Daniel-Griffiths/cloudflare-expo-ota-updates/blob/master/docs/SETUP_WORKER.md#configure-r2-public-access)"
      },
      "MAX_UPDATES_TO_KEEP": {
        "description": "**(Optional)** Number of updates to keep per channel/platform/runtime. Keeps all updates by default."
      },
      "ALLOWED_UPLOAD_IPS": {
        "description": "**(Optional)** Comma-separated list of IPs allowed to upload updates. Leave empty to allow all. It's also **highly** recommended to setup worker [security rules](https://github.com/Daniel-Griffiths/cloudflare-expo-ota-updates/blob/master/docs/SETUP_WORKER.md#secure-the-worker)."
      }
    }
  }
}
